const forms=[('LIQUID', 'flow / adaptation'), ('VAPOR', 'remote / cloud'), ('ICE', 'stability'), ('MIST', 'distribution'), ('WAVE', 'motion'), ('BUBBLE', 'encapsulation'), ('OCEAN', 'whole-system'), ('RAIN', 'input / renewal'), ('RIVER', 'continuous flow'), ('SPRING', 'source / recovery'), ('DEW', 'micro-state'), ('TIDE', 'rhythm'), ('FROST', 'cooling / stability'), ('STORM', 'high-energy state')];const root=document.querySelector("#forms");
forms.forEach(([n,r])=>{const e=document.createElement("div");e.className="form";e.innerHTML=`<strong>${n}</strong><span>${r}</span>`;root.appendChild(e)});
let state={water:0,chaos:0,temp:20,generation:1};function render(){water.textContent=state.water.toFixed(1);chaos.textContent=state.chaos.toFixed(1);temp.textContent=state.temp.toFixed(1)+"°C";generation.textContent=state.generation}
cycle.addEventListener("click",()=>{state.water=Math.max(0,Math.min(10,state.water+Math.random()*2-.4));state.temp=Math.max(-10,Math.min(40,state.temp+Math.random()*4-2));state.chaos=Math.max(0,Math.min(10,state.chaos+(state.water>5?.5:-.2)));state.generation++;render()});render();